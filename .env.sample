# Copy to .env and set values. S3_PATH is the only default used by the app.
S3_PATH=s3://dev-headless-ci/en_model.tar.gz

# Optional: for AWS CLI in container (e.g. IAM role can be used instead)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_DEFAULT_REGION=us-east-1
